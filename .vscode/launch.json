// because im lazy and would like to just press f5 for building
{
  "version": "0.2.0",
  "configurations": [
    {
      "type": "node-terminal",
      "name": "Run Script: build",
      "request": "launch",
      "command": "npm run build; exit;",
      "cwd": "${workspaceFolder}"
    }
  ]
}
